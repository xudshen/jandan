package info.xudshen.jandan.data.model.observable;

import info.xudshen.data.BR;

import com.google.gson.annotations.Expose;
import com.google.gson.annotations.SerializedName;
import info.xudshen.droiddata.dao.IModelObservable;
import java.sql.Timestamp;
import java.util.ArrayList;
import java.util.List;

import android.databinding.Bindable;

import info.xudshen.jandan.domain.model.DuoshuoComment;

// THIS CODE IS GENERATED BY greenDAO, EDIT ONLY INSIDE THE "KEEP"-SECTIONS

// KEEP INCLUDES - put your custom includes here
// KEEP INCLUDES END
/**
 * IModelObservable<"DUOSHUO_COMMENT">
 */
public class DuoshuoCommentObservable extends android.databinding.BaseObservable  implements IModelObservable<DuoshuoComment> {

    private Long id;
    @Expose
    @SerializedName("post_id")
    private String commentId;
    @Expose
    @SerializedName("thread_id")
    private String threadId;
    @Expose
    private String threadKey;
    @Expose
    private String message;
    @Expose
    @SerializedName("created_at")
    private Timestamp date;
    @Expose
    @SerializedName("parent_id")
    private String parentCommentId;
    @Expose
    private String authorId;
    @Expose
    private String authorName;
    @Expose
    private String authorAvatar;
    @Expose
    private String authorUrl;

    // KEEP FIELDS - put your custom fields here
    // KEEP FIELDS END

    public DuoshuoCommentObservable() {
    }

    public DuoshuoCommentObservable(Long id) {
        this.id = id;
    }

    public DuoshuoCommentObservable(Long id, String commentId, String threadId, String threadKey, String message, Timestamp date, String parentCommentId, String authorId, String authorName, String authorAvatar, String authorUrl) {
        this.id = id;
        this.commentId = commentId;
        this.threadId = threadId;
        this.threadKey = threadKey;
        this.message = message;
        this.date = date;
        this.parentCommentId = parentCommentId;
        this.authorId = authorId;
        this.authorName = authorName;
        this.authorAvatar = authorAvatar;
        this.authorUrl = authorUrl;
    }

    public Long getId() {
        return id;
    }

    public void setId(Long id) {
        this.id = id;
    }

    @Bindable
    public String getCommentId() {
        return commentId;
    }

    public void setCommentId(String commentId) {
        if ((this.commentId == null && commentId != null)
                || (this.commentId != null && !this.commentId.equals(commentId))) {
            this.commentId = commentId;
            notifyPropertyChanged(BR.commentId);
        }
    }

    @Bindable
    public String getThreadId() {
        return threadId;
    }

    public void setThreadId(String threadId) {
        if ((this.threadId == null && threadId != null)
                || (this.threadId != null && !this.threadId.equals(threadId))) {
            this.threadId = threadId;
            notifyPropertyChanged(BR.threadId);
        }
    }

    @Bindable
    public String getThreadKey() {
        return threadKey;
    }

    public void setThreadKey(String threadKey) {
        if ((this.threadKey == null && threadKey != null)
                || (this.threadKey != null && !this.threadKey.equals(threadKey))) {
            this.threadKey = threadKey;
            notifyPropertyChanged(BR.threadKey);
        }
    }

    @Bindable
    public String getMessage() {
        return message;
    }

    public void setMessage(String message) {
        if ((this.message == null && message != null)
                || (this.message != null && !this.message.equals(message))) {
            this.message = message;
            notifyPropertyChanged(BR.message);
        }
    }

    @Bindable
    public Timestamp getDate() {
        return date;
    }

    public void setDate(Timestamp date) {
        if ((this.date == null && date != null)
                || (this.date != null && !this.date.equals(date))) {
            this.date = date;
            notifyPropertyChanged(BR.date);
        }
    }

    @Bindable
    public String getParentCommentId() {
        return parentCommentId;
    }

    public void setParentCommentId(String parentCommentId) {
        if ((this.parentCommentId == null && parentCommentId != null)
                || (this.parentCommentId != null && !this.parentCommentId.equals(parentCommentId))) {
            this.parentCommentId = parentCommentId;
            notifyPropertyChanged(BR.parentCommentId);
        }
    }

    @Bindable
    public String getAuthorId() {
        return authorId;
    }

    public void setAuthorId(String authorId) {
        if ((this.authorId == null && authorId != null)
                || (this.authorId != null && !this.authorId.equals(authorId))) {
            this.authorId = authorId;
            notifyPropertyChanged(BR.authorId);
        }
    }

    @Bindable
    public String getAuthorName() {
        return authorName;
    }

    public void setAuthorName(String authorName) {
        if ((this.authorName == null && authorName != null)
                || (this.authorName != null && !this.authorName.equals(authorName))) {
            this.authorName = authorName;
            notifyPropertyChanged(BR.authorName);
        }
    }

    @Bindable
    public String getAuthorAvatar() {
        return authorAvatar;
    }

    public void setAuthorAvatar(String authorAvatar) {
        if ((this.authorAvatar == null && authorAvatar != null)
                || (this.authorAvatar != null && !this.authorAvatar.equals(authorAvatar))) {
            this.authorAvatar = authorAvatar;
            notifyPropertyChanged(BR.authorAvatar);
        }
    }

    @Bindable
    public String getAuthorUrl() {
        return authorUrl;
    }

    public void setAuthorUrl(String authorUrl) {
        if ((this.authorUrl == null && authorUrl != null)
                || (this.authorUrl != null && !this.authorUrl.equals(authorUrl))) {
            this.authorUrl = authorUrl;
            notifyPropertyChanged(BR.authorUrl);
        }
    }


    /**
     * convert entity to entityObservable
     */
    public DuoshuoCommentObservable(DuoshuoComment entity) {
        this.id = entity.getId();
        this.commentId = entity.getCommentId();
        this.threadId = entity.getThreadId();
        this.threadKey = entity.getThreadKey();
        this.message = entity.getMessage();
        this.date = entity.getDate();
        this.parentCommentId = entity.getParentCommentId();
        this.authorId = entity.getAuthorId();
        this.authorName = entity.getAuthorName();
        this.authorAvatar = entity.getAuthorAvatar();
        this.authorUrl = entity.getAuthorUrl();
    }

    @Override
    public Long getModelKey() {
        return this.getId();
    }

    @Override
    public void refresh(DuoshuoComment entity) {
        List<Integer> propertyChanges = new ArrayList<>();
        this.id = entity.getId();
        if ((this.commentId == null && entity.getCommentId() != null)
                || (this.commentId != null && !this.commentId.equals(entity.getCommentId()))) {
            this.commentId = entity.getCommentId();
            propertyChanges.add(BR.commentId);
        }
        if ((this.threadId == null && entity.getThreadId() != null)
                || (this.threadId != null && !this.threadId.equals(entity.getThreadId()))) {
            this.threadId = entity.getThreadId();
            propertyChanges.add(BR.threadId);
        }
        if ((this.threadKey == null && entity.getThreadKey() != null)
                || (this.threadKey != null && !this.threadKey.equals(entity.getThreadKey()))) {
            this.threadKey = entity.getThreadKey();
            propertyChanges.add(BR.threadKey);
        }
        if ((this.message == null && entity.getMessage() != null)
                || (this.message != null && !this.message.equals(entity.getMessage()))) {
            this.message = entity.getMessage();
            propertyChanges.add(BR.message);
        }
        if ((this.date == null && entity.getDate() != null)
                || (this.date != null && !this.date.equals(entity.getDate()))) {
            this.date = entity.getDate();
            propertyChanges.add(BR.date);
        }
        if ((this.parentCommentId == null && entity.getParentCommentId() != null)
                || (this.parentCommentId != null && !this.parentCommentId.equals(entity.getParentCommentId()))) {
            this.parentCommentId = entity.getParentCommentId();
            propertyChanges.add(BR.parentCommentId);
        }
        if ((this.authorId == null && entity.getAuthorId() != null)
                || (this.authorId != null && !this.authorId.equals(entity.getAuthorId()))) {
            this.authorId = entity.getAuthorId();
            propertyChanges.add(BR.authorId);
        }
        if ((this.authorName == null && entity.getAuthorName() != null)
                || (this.authorName != null && !this.authorName.equals(entity.getAuthorName()))) {
            this.authorName = entity.getAuthorName();
            propertyChanges.add(BR.authorName);
        }
        if ((this.authorAvatar == null && entity.getAuthorAvatar() != null)
                || (this.authorAvatar != null && !this.authorAvatar.equals(entity.getAuthorAvatar()))) {
            this.authorAvatar = entity.getAuthorAvatar();
            propertyChanges.add(BR.authorAvatar);
        }
        if ((this.authorUrl == null && entity.getAuthorUrl() != null)
                || (this.authorUrl != null && !this.authorUrl.equals(entity.getAuthorUrl()))) {
            this.authorUrl = entity.getAuthorUrl();
            propertyChanges.add(BR.authorUrl);
        }

        if (propertyChanges.size() == 1) {
            notifyPropertyChanged(propertyChanges.get(0));
        } else if (propertyChanges.size() > 1) {
            notifyChange();
        }
    }

    // KEEP METHODS - put your custom methods here
    // KEEP METHODS END

}
