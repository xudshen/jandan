package info.xudshen.jandan.data.model.observable;

import info.xudshen.data.BR;

import com.google.gson.annotations.Expose;
import com.google.gson.annotations.SerializedName;
import info.xudshen.droiddata.dao.IModelObservable;
import java.util.ArrayList;
import java.util.List;

import android.databinding.Bindable;

import info.xudshen.jandan.domain.model.Author;

// THIS CODE IS GENERATED BY greenDAO, EDIT ONLY INSIDE THE "KEEP"-SECTIONS

// KEEP INCLUDES - put your custom includes here
// KEEP INCLUDES END
/**
 * IModelObservable<"AUTHOR">
 */
public class AuthorObservable extends android.databinding.BaseObservable  implements IModelObservable<Author> {

    private Long id;
    @Expose
    @SerializedName("id")
    private Long authorId;
    @Expose
    private String slug;
    @Expose
    private String name;
    @Expose
    private String firstName;
    @Expose
    private String lastName;
    @Expose
    private String nickName;
    @Expose
    private String url;
    @Expose
    private String description;

    // KEEP FIELDS - put your custom fields here
    // KEEP FIELDS END

    public AuthorObservable() {
    }

    public AuthorObservable(Long id) {
        this.id = id;
    }

    public AuthorObservable(Long id, Long authorId, String slug, String name, String firstName, String lastName, String nickName, String url, String description) {
        this.id = id;
        this.authorId = authorId;
        this.slug = slug;
        this.name = name;
        this.firstName = firstName;
        this.lastName = lastName;
        this.nickName = nickName;
        this.url = url;
        this.description = description;
    }

    public Long getId() {
        return id;
    }

    public void setId(Long id) {
        this.id = id;
    }

    @Bindable
    public Long getAuthorId() {
        return authorId;
    }

    public void setAuthorId(Long authorId) {
        if ((this.authorId == null && authorId != null)
                || (this.authorId != null && !this.authorId.equals(authorId))) {
            this.authorId = authorId;
            notifyPropertyChanged(BR.authorId);
        }
    }

    @Bindable
    public String getSlug() {
        return slug;
    }

    public void setSlug(String slug) {
        if ((this.slug == null && slug != null)
                || (this.slug != null && !this.slug.equals(slug))) {
            this.slug = slug;
            notifyPropertyChanged(BR.slug);
        }
    }

    @Bindable
    public String getName() {
        return name;
    }

    public void setName(String name) {
        if ((this.name == null && name != null)
                || (this.name != null && !this.name.equals(name))) {
            this.name = name;
            notifyPropertyChanged(BR.name);
        }
    }

    @Bindable
    public String getFirstName() {
        return firstName;
    }

    public void setFirstName(String firstName) {
        if ((this.firstName == null && firstName != null)
                || (this.firstName != null && !this.firstName.equals(firstName))) {
            this.firstName = firstName;
            notifyPropertyChanged(BR.firstName);
        }
    }

    @Bindable
    public String getLastName() {
        return lastName;
    }

    public void setLastName(String lastName) {
        if ((this.lastName == null && lastName != null)
                || (this.lastName != null && !this.lastName.equals(lastName))) {
            this.lastName = lastName;
            notifyPropertyChanged(BR.lastName);
        }
    }

    @Bindable
    public String getNickName() {
        return nickName;
    }

    public void setNickName(String nickName) {
        if ((this.nickName == null && nickName != null)
                || (this.nickName != null && !this.nickName.equals(nickName))) {
            this.nickName = nickName;
            notifyPropertyChanged(BR.nickName);
        }
    }

    @Bindable
    public String getUrl() {
        return url;
    }

    public void setUrl(String url) {
        if ((this.url == null && url != null)
                || (this.url != null && !this.url.equals(url))) {
            this.url = url;
            notifyPropertyChanged(BR.url);
        }
    }

    @Bindable
    public String getDescription() {
        return description;
    }

    public void setDescription(String description) {
        if ((this.description == null && description != null)
                || (this.description != null && !this.description.equals(description))) {
            this.description = description;
            notifyPropertyChanged(BR.description);
        }
    }


    /**
     * convert entity to entityObservable
     */
    public AuthorObservable(Author entity) {
        this.id = entity.getId();
        this.authorId = entity.getAuthorId();
        this.slug = entity.getSlug();
        this.name = entity.getName();
        this.firstName = entity.getFirstName();
        this.lastName = entity.getLastName();
        this.nickName = entity.getNickName();
        this.url = entity.getUrl();
        this.description = entity.getDescription();
    }

    @Override
    public Long getModelKey() {
        return this.getId();
    }

    @Override
    public void refresh(Author entity) {
        List<Integer> propertyChanges = new ArrayList<>();
        this.id = entity.getId();
        if ((this.authorId == null && entity.getAuthorId() != null)
                || (this.authorId != null && !this.authorId.equals(entity.getAuthorId()))) {
            this.authorId = entity.getAuthorId();
            propertyChanges.add(BR.authorId);
        }
        if ((this.slug == null && entity.getSlug() != null)
                || (this.slug != null && !this.slug.equals(entity.getSlug()))) {
            this.slug = entity.getSlug();
            propertyChanges.add(BR.slug);
        }
        if ((this.name == null && entity.getName() != null)
                || (this.name != null && !this.name.equals(entity.getName()))) {
            this.name = entity.getName();
            propertyChanges.add(BR.name);
        }
        if ((this.firstName == null && entity.getFirstName() != null)
                || (this.firstName != null && !this.firstName.equals(entity.getFirstName()))) {
            this.firstName = entity.getFirstName();
            propertyChanges.add(BR.firstName);
        }
        if ((this.lastName == null && entity.getLastName() != null)
                || (this.lastName != null && !this.lastName.equals(entity.getLastName()))) {
            this.lastName = entity.getLastName();
            propertyChanges.add(BR.lastName);
        }
        if ((this.nickName == null && entity.getNickName() != null)
                || (this.nickName != null && !this.nickName.equals(entity.getNickName()))) {
            this.nickName = entity.getNickName();
            propertyChanges.add(BR.nickName);
        }
        if ((this.url == null && entity.getUrl() != null)
                || (this.url != null && !this.url.equals(entity.getUrl()))) {
            this.url = entity.getUrl();
            propertyChanges.add(BR.url);
        }
        if ((this.description == null && entity.getDescription() != null)
                || (this.description != null && !this.description.equals(entity.getDescription()))) {
            this.description = entity.getDescription();
            propertyChanges.add(BR.description);
        }

        if (propertyChanges.size() == 1) {
            notifyPropertyChanged(propertyChanges.get(0));
        } else if (propertyChanges.size() > 1) {
            notifyChange();
        }
    }

    // KEEP METHODS - put your custom methods here
    // KEEP METHODS END

}
